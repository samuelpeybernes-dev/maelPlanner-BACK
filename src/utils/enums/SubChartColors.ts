/**
 ** Les constantes à utiliser pour les couleurs, pour avoir une cohérence entre les graphiques (même couleur pour une même data).
 */
export enum SubChartColors {
  ACD = 'ACD',
  SEO = 'SEO',
  SEA = 'SEA',
  DIRECT = 'DIRECT',
  OTHER = 'OTHER',
  EMAIL = 'EMAIL',
  UNKNOWN = 'UNKNOWN',
  INFO_REQUESTS = 'INFO_REQUESTS',
  QUOTE_REQUESTS = 'QUOTE_REQUESTS',
  DEFAULT = '0',
}
